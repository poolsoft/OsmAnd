<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">
	
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>

    <application
        android:icon="@mipmap/icon_nightly"
        tools:replace="android:icon">
        
        <!-- Car Launcher Activity - Ana launcher ekrani -->
        <activity
            android:name="net.osmand.plus.activities.MapActivity"
            android:label="@string/app_name"
            android:theme="@style/FirstSplashScreenNightlyFree"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode"
            android:exported="true"
            tools:replace="android:screenOrientation,android:theme,android:configChanges">
            
            <!-- Launcher olarak calisabilmesi icin -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Media Notification Listener - Muzik kontrolu icin -->
        <service
            android:name="net.osmand.plus.carlauncher.MediaNotificationListener"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="net.osmand.carlauncher.fileprovider"
            tools:replace="android:authorities"/>
    </application>
</manifest> 

